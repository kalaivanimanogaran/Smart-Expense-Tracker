specVersion: "v1"
serviceId: "expensetracker"
location: "us-central1"
schema:
  source: "./schema"
  datasource:
    postgresql:
      database: "fdcdb"
      cloudSql:
        instanceId: "expensetracker-fdc"
      # schemaValidation: "COMPATIBLE"
connectorDirs: ["./connector"]
